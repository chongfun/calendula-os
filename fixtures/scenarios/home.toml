[[steps]]
button = "Back"

[[steps]]
button = "Back"

[expect]
view = "Home"
selection = 0
# Boot itself is the deep Full (asserted via history); the post-boot
# re-render is the one-flicker FastClean.
last_refresh = "FastClean"
history_contains = "refresh Full"
