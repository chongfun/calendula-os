[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
app-core = { path = "../app-core" }
display = { path = "../display" }
proto = { path = "../proto" }
qrcodegen-no-heap = "1.8.1"

[dev-dependencies]
heapless = "0.9.0"
