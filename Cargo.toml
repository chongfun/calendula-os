[workspace]
members = [
    "display",
    "ui",
    "proto",
    "hal-ext",
    "fw",
]
resolver = "2"

[profile.release]
opt-level = "s"          # Size over speed
lto = "fat"
codegen-units = 1
panic = "abort"
